/// -- LIST OF ENUMS --
/// They cannot be created inside a class.

enum TextSize { small, medium, large }


// Example:
// enum OrderStatus { pending, inProgress, completed, cancelled }
// enum PaymentMethod { cash, card, paypal, googlePay, applePay, other }